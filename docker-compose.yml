services:
  srserver:
    build:
      context: ./
      dockerfile: Dockerfile
    image: dl_disgenet
    container_name: dl_disgenet_con
    volumes:
      - .:/home/testuser/
    ports:
      - "2299:22"   #SSH
      - "7860:7860" #GRADIO
    restart: unless-stopped
 #   deploy:
  #    resources:
   #     reservations:
    #      devices:
     #       - driver: "nvidia"
      #        capabilities: [ "gpu" ]
       #       count: 1